{
  "name": "minimax-agent",
  "version": "1.0.0",
  "description": "AI Agent connected to MiniMax-M2.1 model with Zod validation",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node -r dotenv/config src/test.ts",
    "test": "ts-node -r dotenv/config src/test.ts",
    "start": "node dist/test.js",
    "clean": "rm -rf dist"
  },
  "keywords": ["ai", "agent", "minimax", "llm"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/dotenv": "^4.0.3",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.2"
  }
}
